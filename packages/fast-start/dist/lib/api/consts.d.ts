import type { PaginationPayload, SortPayload } from "./types";
export declare const defaultSort: SortPayload;
export declare const defaultFilter: Record<string, any>;
export declare const defaultPagination: PaginationPayload;
